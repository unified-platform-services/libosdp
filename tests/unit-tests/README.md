# Libosdp Tests

Really poor man's unit testing harness.

Run these via `make check` (lean build) or `ctest --test-dir build` (CMake). See
[Running the Test Suite](https://doc.osdp.dev/libosdp/build-and-install#running-the-test-suite)
on doc.osdp.dev for the full flow.
