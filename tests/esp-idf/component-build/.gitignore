# Created by the release workflow (and by hand, per README.md) rather than
# committed: a checked-in symlink to the repo root makes the tree infinitely
# deep to anything that walks it following links.
/components/

# Generated by the ESP-IDF component manager on each configure.
/dependencies.lock
/sdkconfig
/sdkconfig.old
